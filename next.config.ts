import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins : ['local-origin.dev', '*.local-origin.dev', '*.cluster-ulqnojp5endvgve6krhe7klaws.cloudworkstations.dev'],
};

export default nextConfig;
